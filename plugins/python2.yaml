id: 'python2'
author: 'cekout'
name: 'python2'
description: 'Check if python 2 is installed'
severity: 'high'
tests:
  - command: 'python2 --version'
    parsers:
      - parser_type: 'matcher'
        rule_type: 'regex'
        rules:
          - 'Python 2'
